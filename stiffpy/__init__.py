"""
Frame Package, this modules overwrites the class Node, Member, Structure,
Force, Moment, and DistributedForce.

This new classes make easy to construct new 2D frames

No Documentation will be provided for these new classes, we assume that
what they're doing is fairly obvious
"""
